import { StyleSheet } from 'react-native';
import { SCREEN_WIDTH } from '../../constants';

const styles = {
  container: {
    flexDirection:'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
    paddingBottom: 45
  }
};

export { styles };